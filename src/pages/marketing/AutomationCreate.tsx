/**
 * Automation Create/Edit Page
 * 
 * Create or edit an automation workflow.
 * This will be enhanced with custom content support in Phase 3.
 */

import MarketingAutomationCreate from "../MarketingAutomationCreate";

export default function AutomationCreate() {
  // For now, reuse the existing automation creator
  // Will be enhanced with custom content support in Phase 3
  return <MarketingAutomationCreate />;
}
