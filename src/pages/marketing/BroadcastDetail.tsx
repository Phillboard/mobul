/**
 * Broadcast Detail Page
 * 
 * View broadcast analytics and details.
 */

import MarketingCampaignDetail from "../MarketingCampaignDetail";

export default function BroadcastDetail() {
  // For now, reuse the existing campaign detail page
  // Will be enhanced with broadcast-specific analytics later
  return <MarketingCampaignDetail />;
}
